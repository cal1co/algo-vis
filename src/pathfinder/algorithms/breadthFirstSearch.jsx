export const breadthFirstSearch = (nodes, start, finish) => {

}



/**
 * let unvisitedNodes ...
 * while(unvisitedNodes.length > 0){
 * let node = unvisitedNodes.shift()
 * if (node.finish){
 * return true} else {
 * unvisitedNodes.push(...node.children)
 * }
 * }
 */